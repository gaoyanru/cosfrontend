import React from 'react'
import sinon from 'sinon'
import { render, shallow, mount }  from 'enzyme'

describe('test demo', () => {
  it('simulate xhr', () => {
    expect(1).toBe(1)
  })
});
